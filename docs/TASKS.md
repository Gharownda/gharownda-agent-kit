# Task manifests

Task manifests under `agent/tasks/` are checked-in contracts for bounded implementation work.

This document will be expanded by the first qualification task so the public framework documents its own task format using the implementation as the source of truth.
